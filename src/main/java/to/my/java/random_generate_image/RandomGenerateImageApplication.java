package to.my.java.random_generate_image;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "to.my.java.random_generate_image")
public class RandomGenerateImageApplication {
    public static void main(String[] args) {
        SpringApplication.run(RandomGenerateImageApplication.class, args);
    }
}
